<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <header>
        <% include partials/_header %>
    </header>
    <h1>Shorten URL's</h1>
    <% for(let key in urls) { %>
       <li> <%= key %> = <%= urls[key] %></li>
    <% } %>
    <div></div>
    <h2><a href="localhost:8080/urls"> Shrink your URL here</a></h2>
</body>
</html>